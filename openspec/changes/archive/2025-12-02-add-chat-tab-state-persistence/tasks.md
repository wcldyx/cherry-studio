## 1. 实现规划
- [x] 1.1 梳理标签模型所需字段（会话类型、引用 ID、排序、激活状态、固定标记等）并定义持久化 schema。
- [x] 1.2 在聊天标签状态管理中增加读写持久层（本地文件或 indexedDB），确保在标签增删、激活变更时即时更新。
- [x] 1.3 应用启动或聊天页面装载时读取持久化数据，恢复标签集合并对缺失会话执行校验与降级策略。
- [x] 1.4 编写与更新单元/集成测试，覆盖持久化成功、缺失数据回退及跨视图（话题/Session）恢复路径。
